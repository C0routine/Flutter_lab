import 'package:flutter/cupertino.dart';

/// App Style 정의
class AppStyle {
  // margin, padding
  static const double gapBase = 16;
  static const double gapHard = 26;
  static const EdgeInsets gapBaseAll = EdgeInsets.all(16);
  static const EdgeInsets gapHardAll = EdgeInsets.all(26);

  // border
  static const BorderRadius baseBorder = BorderRadius.all(Radius.circular(16));

  // fontSize
  static const double largeTextSize = 25;
  static const double mediumTextSize = 15;
  static const double smallTextSize = 12;
}
